"""scripts.admin — cockpit and runner administration sub-package.

Sub-modules:
  api      — all cockpit data-API functions (cmd_*, _resolve_*, etc.)
  cockpit  — CockpitHTTPServer and HTTP handler
  runner   — runner SSH deploy / bootstrap / config
"""
