"""scripts.sync — Memory Hub sync sub-package.

Sub-modules:
  asset_ops  — connector asset export/import (pipelines, NOTES.md, spans.json)
  git_ops    — git worktree operations (push, pull, merge, conflict resolution)
  sync_flow  — high-level push_flow / pull_flow / event and poll loops
"""
